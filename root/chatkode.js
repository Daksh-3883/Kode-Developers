document.addEventListener('DOMContentLoaded', () => {
  const chatkodePage = document.querySelector('.chatkode-page');
  if (chatkodePage) {
    chatkodePage.setAttribute('data-ready', 'true');
  }
});
